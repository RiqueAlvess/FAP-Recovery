datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// Nenhum model ainda — schema inicializado vazio, aguardando as primeiras
// entidades de domínio (Cliente, Estabelecimento, etc.).
